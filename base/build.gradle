plugins {
    id("io.github.rieske.dbtest.published-library")
    id("java-test-fixtures")
}

description = "Fast tests against containerized databases - base module. Used transitively by concrete implementations like mysql or posgresql."

dependencies {
    api("org.junit.jupiter:junit-jupiter-api:5.9.2")

    testFixturesApi("org.assertj:assertj-core:3.24.2")
}

components.java.withVariantsFromConfiguration(configurations.testFixturesApiElements) { skip() }
components.java.withVariantsFromConfiguration(configurations.testFixturesRuntimeElements) { skip() }
