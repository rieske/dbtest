CREATE TABLE table_one(
    id VARCHAR(36) NOT NULL PRIMARY KEY,
    foo TEXT NOT NULL,
    bar TEXT NOT NULL,
    fizz TEXT NOT NULL,
    buzz TEXT NOT NULL
);

CREATE TABLE table_two(
    id VARCHAR(36) NOT NULL PRIMARY KEY,
    foo TEXT NOT NULL,
    bar TEXT NOT NULL,
    fizz TEXT NOT NULL,
    buzz TEXT NOT NULL
);

CREATE TABLE table_three(
    id VARCHAR(36) NOT NULL PRIMARY KEY,
    foo TEXT NOT NULL,
    bar TEXT NOT NULL,
    fizz TEXT NOT NULL,
    buzz TEXT NOT NULL
);

CREATE TABLE table_four(
    id VARCHAR(36) NOT NULL PRIMARY KEY,
    foo TEXT NOT NULL,
    bar TEXT NOT NULL,
    fizz TEXT NOT NULL,
    buzz TEXT NOT NULL
);

CREATE TABLE table_five(
    id VARCHAR(36) NOT NULL PRIMARY KEY,
    foo TEXT NOT NULL,
    bar TEXT NOT NULL,
    fizz TEXT NOT NULL,
    buzz TEXT NOT NULL
);

CREATE TABLE table_six(
    id VARCHAR(36) NOT NULL PRIMARY KEY,
    foo TEXT NOT NULL,
    bar TEXT NOT NULL,
    fizz TEXT NOT NULL,
    buzz TEXT NOT NULL
);

CREATE TABLE table_seven(
    id VARCHAR(36) NOT NULL PRIMARY KEY,
    foo TEXT NOT NULL,
    bar TEXT NOT NULL,
    fizz TEXT NOT NULL,
    buzz TEXT NOT NULL
);

CREATE TABLE table_eight(
    id VARCHAR(36) NOT NULL PRIMARY KEY,
    foo TEXT NOT NULL,
    bar TEXT NOT NULL,
    fizz TEXT NOT NULL,
    buzz TEXT NOT NULL
);

CREATE TABLE table_nine(
    id VARCHAR(36) NOT NULL PRIMARY KEY,
    foo TEXT NOT NULL,
    bar TEXT NOT NULL,
    fizz TEXT NOT NULL,
    buzz TEXT NOT NULL
);

CREATE TABLE table_ten(
    id VARCHAR(36) NOT NULL PRIMARY KEY,
    foo TEXT NOT NULL,
    bar TEXT NOT NULL,
    fizz TEXT NOT NULL,
    buzz TEXT NOT NULL
);
