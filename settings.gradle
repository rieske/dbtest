pluginManagement {
    includeBuild("gradle/build-logic")
}
dependencyResolutionManagement {
    repositories {
        mavenCentral()
    }
}

rootProject.name = "dbtest"
include(":base")
include(":mysql")
include(":postgresql")
include(":h2")
