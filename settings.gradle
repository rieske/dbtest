pluginManagement {
    includeBuild("gradle/build-logic")
}
dependencyResolutionManagement {
    repositories {
        mavenCentral()
    }
}

rootProject.name = "java-fast-database-tests"
include(":base")
include(":mysql")
include(":postgres")
